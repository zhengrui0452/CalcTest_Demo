
public class CheckResult {

	public boolean CheckResult(String line)
	{
		//todo 
		//1.判断是第一行是否为学号；若是则进入下面的判断，若不是直接返回false
		//2.第2-n行以‘=’分割 
		//3.判断等号左侧是否结果会出现小数;若不是则进入下面的判断，若是小数直接返回false
		//4.判断等号左侧是否等于等号右侧;若相等则返回true，否则返回false
				
		return false;
	}
}
